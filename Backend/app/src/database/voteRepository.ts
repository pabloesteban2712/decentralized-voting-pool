export const createVotePoolDB = async (title: string, description: string, duration: number|void, vote: number|void, userAlreadyVote: boolean): Promise<void> => {
    // await pool.query(
    //     `Insert into users (name, username, mail, password, role_id) values ($1, $2, $3, $4, $5)`,
    //     [name, username, mail, passwordHash, role]   
    // );
};