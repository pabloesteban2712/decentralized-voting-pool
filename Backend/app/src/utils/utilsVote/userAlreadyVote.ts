export async function checkuserAlreadyVote(walletAddress: string) {

    // const vote = await Vote.findOne({ where: { walletAddress }});
    
    // if (vote) {
    //     throw new Error("User has already voted.");
    // }

    return true;
}